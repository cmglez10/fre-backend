// Copy config structure to config.js

import { MainConfig } from './interfaces/config.interfaces';

export const config: MainConfig = {
  db: {
    database: ' ',
    username: ' ',
    password: ' ',
    host: ' ',
    port: 5432
  }
};
